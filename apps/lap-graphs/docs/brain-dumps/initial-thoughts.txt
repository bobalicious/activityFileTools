Strava, in its paid plans, has a really nice graph that will show you an average pace for a lap. This is particularly good for viewing your ability to run intervals. If, for example, you're running 5 three-minute reps with 90 seconds of recovery, you would end up with a bar graph where, along the bottom (the x-axis), time would be shown. Your first three-minute block would be three minutes wide, and the height would be the average pace during that three-minute lap. You would then have a thin 90-second recovery bar, and the height of that would be the average pace of that. I'd like to replicate the ability to create those graphs using the data that's in either Strava or Garmin, using their APIs if there are any. The result should be, for example, in an interval, a rendered graph showing that time. It should, I think, recognise if the pace is way out of whack. It should recognise that that's a rest and ignore it, so you end up with a gap, but you don't end up with a bar, because the bar showing the average pace of your rest is not useful. On top of that, it would be good if we saw a similar graph that could be shown for:
- average cadence
- average stride length (if that's available)
- average heart rate
- a smoothed heart rate graph for that interval  What I would like is an app that will connect to Strava or Garmin (whichever is the simplest one to connect to) and allow us to generate a graph from the data from that session and easily upload it to the right activity on Strava as media. This will probably need a lot of investigation to find out if the APIs are available. If it's simply not feasible to get all that data out, I suspect it is all there. It's just that we need to examine it, and we should have then potentially something like a node app that will allow us to make it with a simple user interface so that we can upload things and so on and so forth, and potentially let you store different kinds of configurations of types of things. I can go, "This is an interval session, so upload that," or "This is a race, upload that," or that kind of thing. The starting point, I think, is the investigation into how this might work and what's available. I'd like you to output a project overview document with the details of what it is that we're trying to achieve. Once we've done that, then move on to an investigation create document for each of the APIs that we have available:
- one for Garmin
- one for Strava, including in the Strava one uploading of media against individual activities, retrieving the activity so we can select which one we're going to put it up against  I'll find, in the most recent activity, if there's information on Garmin that ties a given activity on Garmin to a given activity on Strava, that kind of thing. We're going to run this through writing documents for each bit.